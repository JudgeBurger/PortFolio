{% extends 'base.html.twig' %}

{% block title %}Damien C. | Éditer{% endblock %}

{% block body %}
    <h1>Éditer un Projet</h1>

    <div class="row">
        <div class="col-12">
            {{ include('projects/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <a href="{{ path('projects_index') }}">Retour à la liste des Projets</a>

            {{ include('projects/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
